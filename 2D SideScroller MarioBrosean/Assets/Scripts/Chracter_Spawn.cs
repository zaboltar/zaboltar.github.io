using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chracter_Spawn : MonoBehaviour {

	public GameObject[] players;

	void Start () {
		/*if (Player.Prefs.GetInt ("SellectedCharacter") == 0){
			Instantiate (players [(0)], Vector2.zero, Quaternion.identity);
		}
		if (Player.Prefs.GetInt ("SellectedCharacter") == 1){
			Instantiate (players [(1)], Vector2.zero, Quaternion.identity);
		}
*/
	}
	
	
}
